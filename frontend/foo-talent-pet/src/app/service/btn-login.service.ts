import { Injectable } from '@angular/core';
// import {NgbModal, NgbModalRef} from '@ng-bootstrap/ng-bootstrap';


@Injectable({
  providedIn: 'root'
})
export class BtnLoginService {

  // private modalRef: NgbModalRef | undefined;

  // constructor(private btnLoginService: NgbModal){}

  // openLogin(content:any){
  //    this.modalRef = this.btnLoginService.open(content, {size:'lg', backdrop: 'static', scrollable: true});
  // }

  // closeLogin(){
  //   if (this.modalRef) {
  //     this.modalRef.close();
  //     this.modalRef = undefined;
  //   }
  // }
}
